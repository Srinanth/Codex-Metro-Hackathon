import { ArrowRight, Building2, MessageSquareText, Search } from "lucide-react";
import { motion } from "framer-motion";
import { useNavigate } from "react-router-dom";
import { Brand } from "@/components/brand";
import { Button } from "@/components/ui/button";
const benefits = [{ icon: MessageSquareText, title: "Talk naturally", text: "Ask questions and make requests without navigating forms." }, { icon: Search, title: "Discover nearby businesses", text: "Find a local business and start a conversation in seconds." }, { icon: Building2, title: "Built for every operation", text: "One receptionist experience across services, orders and reservations." }];
export function LandingPage() { const navigate = useNavigate(); return <div className="min-h-screen"><header className="mx-auto flex max-w-7xl items-center justify-between px-5 py-5 md:px-8"><Brand /><Button variant="ghost" onClick={() => navigate("/owner/onboarding")}>For business owners</Button></header><main className="mx-auto max-w-7xl px-5 pb-16 pt-16 md:px-8 md:pt-24"><motion.div initial={{ opacity: 0, y: 16 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: .45 }} className="max-w-3xl"><span className="inline-flex rounded-full border border-border bg-surface px-3 py-1 text-sm text-accent">Meet BizzAI</span><h1 className="mt-6 text-4xl font-semibold tracking-tight md:text-6xl">A better way to talk to local businesses.</h1><p className="mt-6 max-w-2xl text-lg leading-8 text-muted">BizzAI connects you with a business receptionist that understands what you need and handles the rest naturally.</p><div className="mt-10 flex flex-col gap-3 sm:flex-row"><Button size="lg" onClick={() => navigate("/business")}><Search className="mr-2 h-5 w-5" />Search businesses<ArrowRight className="ml-2 h-4 w-4" /></Button><Button size="lg" variant="outline" onClick={() => navigate("/owner/onboarding")}><Building2 className="mr-2 h-5 w-5" />Set up your business</Button></div></motion.div><section className="mt-20 grid gap-4 md:grid-cols-3">{benefits.map(({ icon: Icon, title, text }, index) => <motion.article key={title} initial={{ opacity: 0, y: 12 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: .15 + index * .08 }} className="rounded-xl border border-border bg-surface p-6 shadow-sm"><Icon className="h-6 w-6 text-accent" /><h2 className="mt-5 font-semibold">{title}</h2><p className="mt-2 text-sm leading-6 text-muted">{text}</p></motion.article>)}</section></main><footer className="border-t border-border"><div className="mx-auto flex max-w-7xl flex-col gap-2 px-5 py-7 text-sm text-muted sm:flex-row sm:items-center sm:justify-between md:px-8"><span>© 2026 BizzAI</span><span>AI reception for local businesses</span></div></footer></div>; }
