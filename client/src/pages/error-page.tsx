import { ArrowLeft, CloudOff, Home, ServerCrash } from "lucide-react";
import { Link, useNavigate } from "react-router-dom";
import { Button } from "@/components/ui/button";

export function ErrorPage({ kind = "not-found" }: { kind?: "not-found" | "server" | "network" }) { const navigate = useNavigate(); const content = kind === "network" ? { icon: CloudOff, title: "You appear to be offline", text: "Check your connection, then try again." } : kind === "server" ? { icon: ServerCrash, title: "Something went wrong", text: "We could not complete that request. Please try again." } : { icon: ServerCrash, title: "Page not found", text: "The page you’re looking for doesn’t exist or has moved." }; const Icon = content.icon; return <main className="grid min-h-screen place-items-center px-5"><section className="max-w-md text-center"><div className="mx-auto grid h-14 w-14 place-items-center rounded-xl border border-border bg-surface"><Icon className="h-7 w-7 text-accent" /></div><h1 className="mt-6 text-3xl font-semibold">{content.title}</h1><p className="mt-3 text-muted">{content.text}</p><div className="mt-8 flex justify-center gap-3"><Button variant="outline" onClick={() => navigate(-1)}><ArrowLeft className="mr-2 h-4 w-4" />Go back</Button><Button asChild><Link to="/"><Home className="mr-2 h-4 w-4" />Home</Link></Button></div></section></main>; }
